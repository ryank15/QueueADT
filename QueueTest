package jsjf;

public class QueueTest {
	public static void main(String[] args) {
		QueueADT<String> queue = new LinkedQueue<>();

		queue.enqueue(new String("hi"));
		queue.enqueue("bye");
		queue.enqueue("taco");
		queue.enqueue("blah");
		queue.enqueue("yea");
		System.out.println(queue.toString());

		String x = queue.dequeue();
		System.out.println("a = " + queue.dequeue());

		x = queue.dequeue();
		System.out.println("b = " + queue.dequeue());
		x = queue.dequeue();
		System.out.println("c = " + queue.dequeue());
		x = queue.dequeue();
		System.out.println("d = " + queue.dequeue());
	}
}
